/* auto-generated by NAPI-RS */
/* eslint-disable */
export declare class EsmDatabase {
  /** Open an ESM file asynchronously (blocks on mmap + index build). */
  static openDatabase(path: string): Promise<EsmDatabase>
  fileInfo(): unknown
  listGroups(): unknown
  listTypeRecords(sig: string, offset: number, limit: number): unknown
  recordByFormid(formid: string, resolve: string): unknown
  /** Decode a record by EditorID string. */
  recordByEdid(edid: string): Promise<unknown>
  /** Decode a record by FormID or EditorID (auto-detected). */
  recordById(id: string, resolve: string): Promise<unknown>
  /** Return all records that reference the given FormID hex string. */
  referencedBy(formid: string): Promise<unknown>
  /**
   * Return all records that reference the given FormID or EditorID.
   * depth controls the reverse-reference walk depth (1–6, default 1).
   */
  referencedById(id: string, depth?: number): Promise<unknown>
}
/** Parse a FormID hex string to its display form. */
export declare function parseFormId(s: string): string
